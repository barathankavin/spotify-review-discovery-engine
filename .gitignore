.env
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

data/raw/
data/processed/backups/
# NOTE: vector_store/ is intentionally committed so deployments (Streamlit Cloud /
# HF Spaces) load the prebuilt Chroma index instantly instead of re-embedding the
# whole corpus on a cold start. Cold starts only embed the small weekly delta.
data/processed/analysis_run.log

.streamlit/secrets.toml

.DS_Store
Thumbs.db
